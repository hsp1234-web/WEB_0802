# 思路框架: 這裡放開發、測試、格式化等輔助工具。

# 從根目錄的 requirements.in 導入所有核心依賴
-r requirements.in

# --- 開發與測試工具 ---
pip-tools
pytest
httpx  # 用於在測試中非同步請求我們的 API
ipython # Colab 腳本在本地執行時需要

# --- Pytest 擴展 ---
pytest-aiohttp # 用於測試 aiohttp 應用
pytest-xdist # 用於並行測試，發揮多核心效能
pytest-timeout # 為每個測試設置超時，防止掛起
