# Secondary dependencies that provide additional features.
# These are installed in the background by the server after it has started.
aiohttp==3.12.15
psutil==7.0.0
pytz==2025.2
websockets==12.0
# From features file:
pandas==2.3.1
tabulate==0.9.0
