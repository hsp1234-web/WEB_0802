# Core dependencies needed for the API server to start
# Versions are managed by pip-compile via requirements/base.in and requirements/dev.txt
aiohttp
fastapi
psutil
pydantic-settings
uvicorn[standard]
click # Explicitly add click as a dependency for uvicorn
websockets
pytz
aiofiles
# uv is a global tool now, but keeping it here for clarity
uv==0.1.33
# Transitive dependencies will be handled by the installer
# We list the direct dependencies from the original requirements.in
# and let pip/uv resolve the rest.
# The `[standard]` extra for uvicorn pulls in pyyaml, uvloop, etc.
